x : Int = 5
y : Int = 10
suma : Int = x + y

nombre : String = "Juan"
inicial : Char = 'J'
activo : Bool = true

precio : Float = 19.99
descuento : Float = 0.15

suma_tipada = function(a : Int, b : Int) : Int do
    resultado : Int = a + b
end suma_tipada

mayor_que = function(x : Int, y : Int) : Bool do
    res : Bool = if x > y then true else false end
end mayor_que

calcular_area = function(base : Float, altura : Float) : Float do
    area : Float = base * altura
end calcular_area

Point : struct(x : Int, y : Int) = data with x, y
    rep = struct(x : Int, y : Int)
    
    distancia_origen = function() : Float do
        dx : Int = x
        dy : Int = y
        dist : Int = dx * dx + dy * dy
    end distancia_origen
end Point

Person : struct(nombre : String, edad : Int) = data with nombre, edad
    rep = struct(nombre : String, edad : Int)
    
    es_mayor_edad = function() : Bool do
        mayor : Bool = if edad >= 18 then true else false end
    end es_mayor_edad
end Person

resultado_if : Int = if 5 < 10 then 100 else 0 end

potencia : Int = 2 ** 8
division : Float = 20.0 / 4.0
modulo : Int = 10 % 3

condicion1 : Bool = true and false
condicion2 : Bool = true or false
comparacion : Bool = 5 <= 10


numeros : [Int] = [1, 2, 3, 4, 5]

par : (Int, String) = (42, "respuesta")

Rectangle : struct(ancho : Int, alto : Int) = data with ancho, alto
    rep = struct(ancho : Int, alto : Int)
    
    area = function() : Int do
        resultado : Int = ancho * alto
    end area
    
    perimetro = function() : Int do
        resultado : Int = 2 * ancho + 2 * alto
    end perimetro
end Rectangle

